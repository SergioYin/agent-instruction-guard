# Safe AGENTS.md example

- Run tests with `python -m unittest discover -s tests -v`.
- Do not print tokens, private keys, `.env` contents, or user data.
- Ask for approval before destructive filesystem changes.
